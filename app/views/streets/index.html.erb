<h1>Listing streets</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Startlocation</th>
    <th>Endlocation</th>
    <th>Description</th>
    <th>Wikipedialink</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @streets.each do |street| %>
  <tr>
    <td><%= street.name %></td>
    <td><%= street.startLocation %></td>
    <td><%= street.endLocation %></td>
    <td><%= street.description %></td>
    <td><%= street.wikipediaLink %></td>
    <td><%= link_to 'Show', street %></td>
    <td><%= link_to 'Edit', edit_street_path(street) %></td>
    <td><%= link_to 'Destroy', street, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Street', new_street_path %>
